vector_t x (4);
    // for (int i = 0; i < 4; i++) {
    //     x[i] = 1./3.*i;
    // }
    // std::cout << lagrange(f, x) << std::endl;
    // std::cout << newtonlagrange(f,x) << std::endl;
    // std::cout << hermite(f, df, vector_t{0.,1.}) << std::endl;
    // std::cout << hermite(f, df, vector_t{0., 0.5, 1.}) << std::endl;
    // x.resize(5, 0.);
    // for (int i = 0; i < 5; i++) {
    //     x[i] = 1./4.*i;
    // }
    // std::cout << lagrange(f, x) << std::endl;
    // std::cout << newtonlagrange(f, x) << std::endl;
    // std::cout << chebyshev(g, 10, -5., 5.) << std::endl;